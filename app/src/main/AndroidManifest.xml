<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.restaurantmanagementapplicationse">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Main Login Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Guest Activities -->
        <activity
            android:name=".GuestMenuActivity"
            android:exported="false"
            android:label="Menu"
            android:screenOrientation="portrait" />

        <activity
            android:name=".GuestReservationActivity"
            android:exported="false"
            android:label="Reservation"
            android:screenOrientation="portrait" />

        <activity
            android:name=".GuestReservationSuccessActivity"
            android:exported="false"
            android:label="Reservation Details"
            android:screenOrientation="portrait" />

        <!-- Staff Activities -->
        <activity
            android:name=".StaffMenuActivity"
            android:exported="false"
            android:label="Staff Menu"
            android:screenOrientation="portrait" />

        <activity
            android:name=".StaffReservationActivity"
            android:exported="false"
            android:label="Staff Reservations"
            android:screenOrientation="portrait" />

        <!-- Common Activities -->
        <activity
            android:name=".NotificationActivity"
            android:exported="false"
            android:label="Notifications"
            android:screenOrientation="portrait" />

    </application>

</manifest>